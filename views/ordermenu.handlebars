<ul>
        <li><a href="employees">Employees</a></li>
        <li><a href="orders">Orders</a></li>
        <li><a href="menuitems">Menu Items</a></li>
        <li><a href="payments">Payments</a></li>
        <li><a href="ordermenu">OrderMenus</a></li>
        <li style="float:right"><a  href="/">Restaurant POS</a></li>
</ul>



{{!-- Create a table --}}
<table>

    {{!-- Header section --}}
    <thead>

        {{!-- For just the first row, we print each key of the row object as a header cell so we
        know what each column means when the page renders --}}
        <tr>
            {{#each data.[0]}}
            <th>
                {{@key}}
            </th>
            {{/each}}
        </tr>
    </thead>

    {{!-- Body section --}}
    <tbody>

        {{!-- For each row, print the id, fname, lname, homeworld and age, in order --}}
        {{#each data}}
        <tr>
            <td>{{this.orderMenuID}}</td>
            <td>{{this.orderID}}</td>
            <td>{{this.menuID}}</td>
        </tr>
        {{/each}}
    </tbody>
</table>


<h3>Search by order number</h3>
<form id="search-ordermenu-html" method='GET' action='/ordermenu'>
    <label for="searchOrderNumber">Search by Last Name: </label>
    <input type="text" name="searchOrderNumber" id="searchOrderNumber">
    
    <input type="submit">
    <a href='/'><button>Reset</button></a>
</form>

